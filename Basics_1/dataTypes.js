"use strict"; // treat all js code as newer version
/* but today we do not need to write coz by default js file runs in strict mode */

// alert(3+3) this will give error coz we are in node not in broswer

// Primitive
    // number => 2 to power 53
    // bigInt used in stock ,big websites
    // string  = ""
    // boolean => true/false
    // null => standalone [ek represnataion hai empty ka intentionally aap bata rahe ho ki it is empty ] specaial value let sth = null;  
    // undefined => abhi value nhi di hai
    // symbol => used for uniquness mostly , will know in react

// object
    // will talk later

console.log(typeof "ben");
console.log(typeof age);
console.log(typeof null); //object, null is an object
console.log(typeof undefined); //undefined

